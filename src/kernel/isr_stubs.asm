[BITS 32]

; TODO: Define ISR stubs and common interrupt handler linkage.
; These stubs will push registers and jump to a common C handler.

global isr_stub_table

isr_stub_table:
    ; Placeholder table; real ISR stubs should be added here.
