#!/usr/bin/env bash
# Placeholder helper to build the MiniOS image.
# The Makefile already drives the build; this script exists for future tooling.

set -euo pipefail

echo "Use 'make' to build and 'make run' to launch MiniOS under QEMU."
